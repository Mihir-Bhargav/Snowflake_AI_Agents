"""Synthetic banking data generators (Phase 1 source systems).

Produces internally-consistent, deterministic data matching the Bronze schemas in
docs/DATA_MODEL.md §1. Swapping these for real sources is a config change (the source
registry), not a code change.
"""
