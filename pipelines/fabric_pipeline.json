{
  "_comment": "Microsoft Fabric Data Pipeline definition (template). Schedules the nightly run and triggers the run_nightly notebook. Import into a Fabric workspace and bind the referenced notebook + Key Vault. See docs/DEPLOYMENT.md §C.",
  "name": "banking-ai-nightly",
  "properties": {
    "description": "Nightly Extract->Transform->Aggregate->Insight run for the banking analytics platform.",
    "activities": [
      {
        "name": "RunNightlyNotebook",
        "type": "TridentNotebook",
        "typeProperties": {
          "notebookId": "<bind: notebook 'run_nightly'>",
          "parameters": {
            "as_of": { "value": "@formatDateTime(adddays(utcNow(), -1), 'yyyy-MM-dd')", "type": "string" },
            "mcp_url": { "value": "@pipeline().globalParameters.mcp_url", "type": "string" }
          }
        },
        "policy": { "timeout": "01:00:00", "retry": 1, "retryIntervalInSeconds": 300 }
      }
    ],
    "policy": {
      "onFailure": "alert via Fabric activity monitor / configured notification"
    }
  },
  "schedule": {
    "_comment": "Once daily. Adjust startTime/timeZone to the bank's reporting cutoff.",
    "frequency": "Day",
    "interval": 1,
    "startTime": "2026-06-01T02:00:00",
    "timeZone": "W. Europe Standard Time"
  },
  "globalParameters": {
    "mcp_url": { "type": "string", "value": "https://banking-ai-mcp.<region>.azurecontainerapps.io" }
  }
}
